version: '3.9'
services:
  ###
  # PROD
  ###
  api:
    image: renangalvao/repo:project-api
    deploy:
      restart_policy:
        condition: on-failure
        max_attempts: 5
        window: 120s
    ports:
      - :3000
    environment:
      - ACCESS_TOKEN_JWT_SECRET
      - REFRESH_TOKEN_JWT_SECRET
      - MAIL_HOST
      - MAIL_PORT
      - MAIL_USER
      - MAIL_PASSWORD
      - MAIL_FROM
      - DATABASE_URL
      - REDIS_HOST
      - REDIS_PORT
    networks:
      - project_network
    volumes:
      - project_api:/usr/src/app/files
      - project_web_root:/var/www/html
    depends_on:
      - db
      - cache

  cache:
    image: redis:7-alpine
    deploy:
      restart_policy:
        condition: on-failure
        max_attempts: 5
        window: 120s
    ports:
      - :6379
    networks:
      - project_network

  prisma-migrate:
    image: renangalvao/repo:project-prisma-migrate
    ports:
      - :5432
    environment:
      - DATABASE_URL
    depends_on:
      - db
    networks:
      - project_network

  db:
    image: postgres:13
    deploy:
      restart_policy:
        condition: on-failure
        max_attempts: 5
        window: 120s
    ports:
      - :5432
    environment:
      - POSTGRES_USER
      - POSTGRES_PASSWORD
      - POSTGRES_DB
    networks:
      - project_network
    volumes:
      - project_db:/var/lib/postgresql/data

  ###
  # DEV
  ###
  dev-redis:
    image: redis:7-alpine
    deploy:
      restart_policy:
        condition: on-failure
        max_attempts: 5
        window: 120s
    ports:
      - 6379:6379
    networks:
      - project_network

  dev-db:
    image: postgres:13
    deploy:
      restart_policy:
        condition: on-failure
        max_attempts: 5
        window: 120s
    ports:
      - 5432:5432
    env_file:
      - .env.dev
    networks:
      - project_network
    volumes:
      - project_db:/var/lib/postgresql/data


networks:
  project_network:
    driver: bridge

volumes:
  project_api:
  project_db:
  project_web_root:
